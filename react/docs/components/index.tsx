import React, { FC } from "react";
import Container from "../../lib/container";

const Components: FC = () => (
    <Container>
        <h1>Components</h1>
    </Container>
);

export default Components;
